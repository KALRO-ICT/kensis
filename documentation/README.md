# Server setup

The Kensis server has multiple purposes

- a postgres database server 
- a NGINX service hosting the kensis website (port 80/443)
- a mapserver instance providing access to various data files (mapped to path /ows)
- a terria-js instance for advanced map viewing options (mapped to path /maps)